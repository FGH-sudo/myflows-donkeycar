apiVersion: apps/v1
kind: Deployment
metadata:
  name: myflows-infer
  labels:
    app: myflows-infer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myflows-infer
  template:
    metadata:
      labels:
        app: myflows-infer
    spec:
      containers:
        - name: infer
          image: myflows-infer:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: SERVE_MODE
              value: grpc
            - name: MODEL_PATH
              value: /models/myflow_resnet18_best.onnx
            - name: PORT
              value: "50051"
            - name: DEVICE
              value: cuda
            - name: NVIDIA_VISIBLE_DEVICES
              value: all
            - name: NVIDIA_DRIVER_CAPABILITIES
              value: compute,utility
          ports:
            - containerPort: 50051
              name: grpc
          resources:
            limits:
              nvidia.com/gpu: 1
          volumeMounts:
            - name: models
              mountPath: /models
              readOnly: true
      volumes:
        - name: models
          hostPath:
            path: /path/to/mycar/models
            type: Directory
