apiVersion: v1
kind: Service
metadata:
  name: myflows-infer
spec:
  type: NodePort
  selector:
    app: myflows-infer
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
      nodePort: 30051
